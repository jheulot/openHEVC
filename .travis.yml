language: c


os: linux

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.9
      - g++-4.9
      - yasm

before_install:
  - export cc="gcc-4.9"

script:
  - 


jobs:
  include:
    - stage: Build
      script: sh build.sh
    - stage : Do Conformance Tests
      script: sh test.sh
